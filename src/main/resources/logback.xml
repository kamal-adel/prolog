<configuration>
  <logger name="ch.qos.logback.classic.LoggerContext" level="ERROR"/>
  <logger name="org.hibernate.validator.internal.util.Version" level="OFF" />
  <logger name="org.springframework.boot.web.embedded.tomcat.TomcatWebServer" level="OFF" />
  <logger name="org.apache.coyote.http11.Http11NioProtocol" level="OFF" />
  <logger name="org.apache.catalina.core" level="OFF" />

  <appender name="mask" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
      <layout class="com.prolog.masking.MaskingPatternLayout">
        <maskPattern>\"ssn\"\s*:\s*\"(.*?)\"</maskPattern> <!-- SSN JSON pattern -->
        <maskPattern>"x-api-key"\s*:\s*"(.*?)"</maskPattern> <!-- x-api-key field pattern -->
        <maskPattern>"password"\s*:\s*"(.*?)"</maskPattern> <!-- Password field pattern -->
        <maskPattern>"authorization"\s*:\s*"Bearer\s*(.*?)\"</maskPattern> <!-- Authorization token pattern -->
        <pattern>%-5p [%d{ISO8601,UTC}] [%thread] %c: %m%n%rootException</pattern>
      </layout>
    </encoder>
  </appender>

  <root level="info">
    <appender-ref ref="mask"/>
  </root>
</configuration>
